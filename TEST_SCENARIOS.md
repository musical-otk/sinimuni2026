# 쿠폰/도장판 테스트 시나리오 (sinimuni2026)

증빙프리패스·더블/트리플 적립·2만원 현장 예매 쿠폰 추가 기능 및 기존 도장판/쿠폰
로직에 대한 테스트 시나리오. Playwright로 실제 앱 함수(`saveRecord`, `editRecord`,
`deleteCurrentRecord`, `addStamp` 등)를 호출해 검증했다.

테스트 스크립트: `test_coupon_stamp.mjs`, `test_paid_diff_toggle.mjs` (세션 스크래치패드, 재사용 시 재작성 필요)

마지막 검증: 2026-07-12 — **29개 시나리오 전부 통과**

---

## A. 도장판 연동 타이밍

| # | 시나리오 | 결과 |
|---|---|---|
| A1 | 신규 기록을 도장판 미연동으로 저장 → 이후 편집에서 도장판 선택 시 정상 반영 | ✅ PASS |

## B. 도장판 변경

| # | 시나리오 | 결과 |
|---|---|---|
| B3 | 기록을 도장판1에 연동 → 편집으로 도장판2로 변경 → 도장판1에서 이력 제거, 도장판2에 추가, 카운트 재계산 정확 | ✅ PASS |

## C. 더블/트리플 적립 토글

| # | 시나리오 | 결과 |
|---|---|---|
| C5 | 더블 적립 체크 후 저장 → 쿠폰 1개 차감 | ✅ PASS |
| C6 | 같은 기록을 재편집 → 체크박스 그대로 두고 저장(무변경) → 중복 차감 없어야 함 | ✅ PASS (버그 수정 후) |
| C7 | 더블 적립 체크 해제 후 저장 → 쿠폰 원복 | ✅ PASS (버그 수정 후) |
| C8 | 도장 수량 2개(더블 체크)→3개로 변경 시 더블 체크 자동 해제 + 트리플 그룹 노출 | ✅ PASS |

**🐛 발견 및 수정된 버그**: `saveRecord()`가 편집 여부와 무관하게 `usedDoubleStamp`/
`usedTripleStamp`가 true면 매번 무조건 "사용" 이력을 push하고 있었음. 이 때문에
① 체크박스를 안 건드리고 재저장만 해도 중복 차감되고, ② 체크 해제해도 원복이
안 됐음. `prevRecord`와 비교해서 상태가 실제로 바뀔 때만 사용/복원 이력을
push하도록 수정 (commit `f24b268`).

## D. 증빙프리패스 토글 (대조군)

| # | 시나리오 | 결과 |
|---|---|---|
| D9 | 40%/50% 할인권 + 증빙프리패스 병행 체크 → 저장 → 재편집 → 해제 → 저장 → 정상 원복 | ✅ PASS |

`useDiscountPass`는 `record` 필드를 직접 읽어 실시간 계산하는 구조라 더블/트리플
적립과 달리 애초에 원복 문제가 없음 (설계 검증용 대조군).

## E. 삭제 시 원복

| # | 시나리오 | 결과 |
|---|---|---|
| E10 | 더블 적립 사용 + 도장판 연동 기록 삭제 → 도장판 이력 제거 + 더블 적립 쿠폰 원복 | ✅ PASS |
| E11 | 트리플 적립도 동일하게 원복 | ✅ PASS |

## F. 도장판 오버플로우

| # | 시나리오 | 결과 |
|---|---|---|
| F12 | 도장판이 꽉 찬 상태에서 신규 기록 저장 시 오버플로우 → 새 도장판 자동 생성 + 이월 | ✅ PASS |
| F12-1 | 오버플로우된 기록을 삭제했을 때 원래 도장판 + 새 도장판 양쪽 모두 원복 | ✅ PASS |

## G. 쿠폰 잔여 부족

| # | 시나리오 | 결과 |
|---|---|---|
| G13 | 더블/트리플 적립 쿠폰 보유 0개일 때 도장 수량 2/3 선택해도 체크박스 비노출 | ✅ PASS |

## H. 2만원 현장 예매

| # | 시나리오 | 결과 |
|---|---|---|
| H16 | 2만원 현장 예매 선택 후 저장 시 onsiteTicket 쿠폰에 정상 매핑 및 차감 | ✅ PASS |

40%/50% 할인권과 동일하게 "수량 관리" 대상이라 재고 없이는 저장이 막힘 (의도된 동작).

## I. 마일스톤 자동 발급

| # | 시나리오 | 결과 |
|---|---|---|
| I17 | 도장 3칸 달성 시 40% 할인권 자동 발급 | ✅ PASS |
| I18 | 도장 4칸 달성 시 50% 할인권 자동 발급 | ✅ PASS |
| I19 | 도장 7칸(couponKey null, 실황 OST) 달성 시 크래시 없이 정상 처리 | ✅ PASS |

## J. 편집 시 도장판 초과(오버플로우)

| # | 시나리오 | 결과 |
|---|---|---|
| J21 | 편집으로 stampQty를 늘려 도장판 초과 발생 시 새 도장판 자동 생성 여부 (신규생성 흐름과 동일해야 함) | ✅ PASS (버그 수정 후) |
| J21-1 | 편집 중 발생한 오버플로우 기록을 삭제했을 때 양쪽 도장판 모두 원복 | ✅ PASS |

**🐛 발견 및 수정된 버그**: 신규 기록 저장(`saveRecord` 신규분기)과 직접 도장찍기
(`addStamp`)는 도장판 초과 시 confirm 후 새 도장판을 만들어 이월하는데, **기록
편집 흐름만 확인 절차 없이 초과분을 조용히 잘라버리고 있었음** (예: 남은 칸 1개인
도장판에 stampQty를 3으로 편집하면 2개가 아무 경고 없이 유실). 편집 흐름도
`confirm()` + `createNewStampBoard()`로 동일하게 처리하도록 통일 (commit `b53761a`).

## K. 도장판 연동 해제 시 정합성

| # | 시나리오 | 결과 |
|---|---|---|
| K22 | 더블 적립 사용 중인 기록을 편집해서 도장판 연동을 해제(`''`)하면 `usedDoubleStamp`가 강제로 false 처리되고 쿠폰이 원복되는지 | ✅ PASS |

## L. 40%/50% 할인권 & 증빙프리패스 상호작용

| # | 시나리오 | 결과 |
|---|---|---|
| L23 | 40% 할인권 사용 기록 삭제 시 discount40 카운트 자동 복원 (live-computed 대조군) | ✅ PASS |
| L24 | 40%+증빙프리패스 병행 1건, 50%+증빙프리패스 병행 1건 → discountPass 사용량 합산 정확 | ✅ PASS |

## M. 수동 쿠폰 추가/차감 패널 (신규 4종)

| # | 시나리오 | 결과 |
|---|---|---|
| M26 | 증빙프리패스/더블/트리플/현장예매 각각 획득 2개 → 사용 1개 → count 정확 | ✅ PASS (4종 모두) |
| M27 | 수동 이력 삭제(✕ 버튼) 시 count 재계산 정확 | ✅ PASS |

## N. 동시 사용 누적

| # | 시나리오 | 결과 |
|---|---|---|
| N28 | 같은 도장판에서 서로 다른 두 기록이 각각 더블 적립 사용 → 쿠폰/도장 누적 정확 | ✅ PASS |

## O. 차액 지불 라디오 버튼 해제

| # | 시나리오 | 결과 |
|---|---|---|
| O29-1 | 차액 지불(40%) 라디오 클릭 → 체크됨 | ✅ PASS |
| O29-2 | 같은 라디오를 다시 클릭 → 해제되어 "일반 사용" 상태로 복귀 | ✅ PASS (버그 수정 후) |
| O29-3 | 해제 후 다시 클릭 → 재선택 가능 | ✅ PASS |
| O29-4 | 다른 옵션(재관람/기타) 클릭 → 기존 선택(40%) 해제되고 새 옵션 선택 | ✅ PASS |
| O29-5 | 선택된 옵션을 다시 클릭 → 해제 | ✅ PASS |
| O29-6 | 아무 라디오도 선택 안 된 상태로 저장 → `record.paidDiff === false`로 정상 저장, 할인권 정상 차감(일반 사용) | ✅ PASS |

**🐛 발견 및 수정된 버그**: `rec-paid-diff` 라디오 버튼(재관람/기타, 40%)은 네이티브
특성상 한 번 선택하면 재클릭으로 해제할 수 없어, 차액 지불을 선택했다가 다시
"일반 사용"으로 되돌리려면 할인권종을 다른 값으로 바꿨다가 되돌아오는 우회
방법밖에 없었음. `mousedown`으로 클릭 직전 체크 상태를 기록해두고, `click` 시
이미 체크돼 있던 라디오면 강제로 해제하는 방식(`paidDiffMouseDown`/
`paidDiffClick`)으로 재클릭 해제를 지원 (commit `471000a`).

---

## 아직 다루지 않은 영역 (후속 후보)

- 도장판 일시정지/재개 상태와 더블·트리플 적립 체크박스 노출 상호작용
- 캘린더 필터·통계 탭에서 신규 쿠폰 필드(useDiscountPass 등) 반영 여부
- 여러 도장판이 동시에 active 상태일 때 도장판 선택 select의 정렬/표시 정확성
- localStorage 마이그레이션(구버전 데이터 → 신규 쿠폰 필드 기본값) 시나리오
